# Parse a single file through all 5 passes

Parse a single file through all 5 passes

## Usage

``` r
parse_file(file_row)
```

## Arguments

- file_row:

  One-row sb_file tibble (from load_file)

## Value

Named list with keys: file, contexts, symbols_partial, references,
issues
