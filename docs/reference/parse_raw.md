# Parse a file and attach parse data

Parse a file and attach parse data

## Usage

``` r
parse_raw(file_row)
```

## Arguments

- file_row:

  One-row sb_file tibble (code must be populated)

## Value

Named list: list(file = updated sb_file row, issues = sb_issue tibble)
