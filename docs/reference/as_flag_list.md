# Wrap a character vector into a list column value

Wrap a character vector into a list column value

## Usage

``` r
as_flag_list(x = character())
```

## Arguments

- x:

  Character vector or NULL

## Value

List of length 1 containing x (suitable for list columns)
