# Valid symbol types Note: "module_def" and "module_instance" are reserved for future module support but are not produced by the current resolver.

Valid symbol types Note: "module_def" and "module_instance" are reserved
for future module support but are not produced by the current resolver.

## Usage

``` r
SYMBOL_TYPES
```

## Format

An object of class `character` of length 10.
