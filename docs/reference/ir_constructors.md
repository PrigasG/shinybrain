# Internal Representation Constructors

These functions create empty, typed tibbles for each IR object. They are
the contract that all pipeline layers read and write. Call validate_ir()
to verify conformance.
